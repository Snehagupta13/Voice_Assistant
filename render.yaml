services:
  - type: web
    name: voice-assistant
    env: python
    root: .
    buildCommand: uv sync --compile-bytecode
    startCommand: uv run --no-sync uvicorn main:app --host 0.0.0.0 --port $PORT
